bundle:
  name: azure-databricks-market-risk-intelligence

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Shared/.bundle/${bundle.name}/${bundle.target}

include:
  - resources/*.yml

